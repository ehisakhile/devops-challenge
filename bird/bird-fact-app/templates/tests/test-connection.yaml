apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bird-fact-app.fullname" . }}-test-connection"
  labels:
    {{- include "bird-fact-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bird-fact-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
